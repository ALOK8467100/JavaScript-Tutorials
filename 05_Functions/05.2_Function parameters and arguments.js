

let invitation = function(name = "defaultName", time = "night"){  // here we declare function parameter called name 
    console.log(`Welcome! ${name} You are invited on our even at ${time}`)
}

invitation("Marie", "afternoon") // here we declare function argument.
invitation("Alok", "morning")  // here we declare function argument.

/*
now see agrument and parameter
Parameter is name which we declare in function 
Parameter is local variable having scope inside the function
Agrument is value which we pass in our function
*/